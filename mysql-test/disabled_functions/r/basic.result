# The plugin is loaded at startup by suite.opt.
SELECT PLUGIN_STATUS FROM INFORMATION_SCHEMA.PLUGINS
WHERE PLUGIN_NAME='disabled_functions';
PLUGIN_STATUS
ACTIVE
SELECT @@disabled_functions_list;
@@disabled_functions_list
SLEEP,COLUMN_LIST
# Functions named in disabled_functions_list are unavailable.
# Other native functions still resolve normally.
SELECT ABS(-2);
ABS(-2)
2
